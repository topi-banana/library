[package]
name = "verify"
version.workspace = true
edition.workspace = true
publish.workspace = true
repository.workspace = true
license.workspace = true

# 各バイナリがジャッジの 1 問に対応する。
# ここに並ぶ依存は「どれか 1 つの verify で使われていればよい」ため、
# crate 全体としては未使用にならない。
[dependencies]
rle = { path = "../crates/rle" }
